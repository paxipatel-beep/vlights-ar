# ── vlights AR Dashboard — Environment Config ──
# Copy this to .env and fill in your values

PORT=3000
NODE_ENV=production

# Dashboard password — shared with your whole team
DASHBOARD_PASSWORD=Prakash_9892284364

# Session secret — auto-generated by deploy.sh, or run:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=REPLACE_WITH_RANDOM_64_CHAR_STRING

# Database file path (auto-created)
DB_PATH=./data/vlights.db
